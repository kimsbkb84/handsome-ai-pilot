# 1. 보안 파일 (절대 올리면 안 됨!!!)
.env
apikey.txt
secrets.json
*.pem

# 2. 가상환경 폴더 (용량이 크고, 팀원마다 다시 깔아야 함)
venv/
.venv/
env/

# 3. 파이썬 자동 생성 파일 (필요 없는 캐시 파일들)
__pycache__/
*.pyc

# 4. 데이터베이스 및 데이터 (로컬에만 저장할 것들)
chroma_db/
*.db
*.sqlite3

# 5. 업로드된 이미지들 (테스트용 이미지가 섞이면 안 되니까)
uploaded_images/*
!uploaded_images/.gitkeep

# 6. 운영체제/에디터 잡동사니
.DS_Store
.vscode/
.idea/